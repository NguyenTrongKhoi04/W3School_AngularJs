/**
 * ? Data binding in AngularJS is the synchronization between the model and the view.
 * * ng-controller
 *      +> Because of the immediate synchronization of the model and the view, the controller can be
 *              completely separated from the view, and simply concentrate on the model data. Thanks to
 *              the data binding in AngularJS, the view will reflect any changes made in the controller.
 *      +> đọc thêm ở https://www.w3schools.com/angular/angular_controllers.asp
 */
